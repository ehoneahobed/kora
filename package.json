{
  "name": "kora-monorepo",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "benchmark:gates": "pnpm --filter @kora/store test -- src/benchmarks/performance-gates.test.ts && pnpm --filter @kora/merge test -- src/benchmarks/performance-gates.test.ts && pnpm --filter @kora/sync test -- src/benchmarks/performance-gates.test.ts",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@changesets/cli": "^2.27.12",
    "tsup": "^8.3.6",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
