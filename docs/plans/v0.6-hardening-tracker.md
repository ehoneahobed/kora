# v0.6.0 Hardening Tracker

Target release: **0.6.0 public beta** (do not publish until exit criteria met).

## Phase 0 — Scope lock

- [x] Tracking doc (this file)
- [x] Version locked at 0.6.0 across linked packages

## Phase 1 — P0 correctness

- [x] `AppNotReadyError` moved to `@korajs/core`
- [x] Removed `__pending__` query placeholder; `where()` throws before `app.ready`
- [x] Per-app `QueryStoreCache` on `KoraApp.getQueryStoreCache()` + provider context
- [x] `AuthSyncCoordinator` serializes auth-driven reconnects
- [x] Vue `KoraProvider` uses setup-time `provide` + `shallowRef` context
- [x] Rich text `destroy()` idempotent; Vue removes double dispose

## Phase 2 — P1 credibility

- [x] Sync status hooks avoid duplicate event wiring when bridge exists
- [x] Shared `createMutationController` in `@korajs/core/bindings`
- [x] Auth session bindings exported from `@korajs/auth`
- [x] Svelte `KoraStoreProvider` for store-only apps
- [x] Sync query subset warns when non-equality filters are omitted

## Phase 3 — Tests & docs

- [x] Framework hooks migrated to shared mutation controller
- [x] React `OrgProvider` client prop change handling
- [x] Svelte `useOrgMembers` orgId reactivity
- [x] Context errors/docs prefer `KoraProvider` / `KoraStoreProvider`

## Phase 4 — Modularize `create-app`

- [x] `collection-accessor.ts` extracted
- [x] `auth-sync-coordinator.ts` extracted
- [x] `initialize-app.ts` — store open + sync engine construction
- [x] `sync-lifecycle.ts` — reconnection, quality, auth coordinator wiring
- [x] `sync-control.ts` — `app.sync` control surface
- [x] `create-sync-transport.ts` — transport factory
- [x] `setup-devtools.ts` — DevTools instrumentation bootstrap
- [x] `wire-sync-event-forwarding.ts` — `onSyncEvent` forwarding
- [x] `transaction-executor.ts` — shared transaction/mutation runner
- [x] `sequences-accessor.ts` — offline sequence API

`create-app.ts` is now a thin orchestrator (~150 lines) that wires the modules above.

## Phase 5 — Performance polish

- [x] Query cache keys scoped by store name
- [x] Collaborator snapshot structural compare (replaces JSON.stringify)

## Phase 6 — Release gates

- [x] Full `pnpm build` green
- [x] Full `pnpm test` green (30/30 turbo tasks)
- [x] Publish checklist — [npm-publish-checklist.md](../releases/npm-publish-checklist.md)
- [x] `pnpm changeset publish` — **0.6.0 live on npm** (2026-06-20)
- [ ] Post-publish smoke: `npx create-kora-app` on clean machine
- [ ] Git tag `v0.6.0` + GitHub release notes

## Post-hardening cleanup

- [x] Canonical `AuthSyncBinding` in `@korajs/core/bindings`; `KoraAuthSyncBinding` deprecated alias in `@korajs/auth`
- [x] Svelte docs/errors prefer `<KoraProvider>` over `initKoraProvider`
- [x] `sync-lifecycle` removes browser `online` listener on teardown
- [x] Vue `usePresence` watches field values, not whole `user` object identity
- [x] Svelte rich-text tests no longer clear global query cache
